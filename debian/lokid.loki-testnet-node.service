[Unit]
Description=Loki network node
After=network-online.target
Requires=loki-testnet-storage.service
Before=loki-testnet-storage.service

[Service]
User=_loki
Type=simple
WorkingDirectory=/var/lib/loki
Restart=always
RestartSec=5s
ExecStart=/usr/bin/lokid --non-interactive --config-file /etc/loki/testnet.conf
# Enable coloured daemon output:
Environment=TERM=xterm

[Install]
WantedBy=multi-user.target

